# 算法工程师个人简历

一个精美的算法工程师个人简历网站，基于 React 19 + TypeScript + Vite + Tailwind CSS 构建。

## 功能特点

- 🎨 精美的响应式设计，支持移动端和桌面端
- ⚡ 基于 Vite 构建，极速开发和部署
- 🎯 专为算法工程师设计的简历模板
- 📱 完美适配各种设备屏幕
- 🌈 使用 Tailwind CSS 和 shadcn/ui 组件库
- 🚀 一键部署到 GitHub Pages

## 技术栈

- **框架**: React 19
- **语言**: TypeScript
- **构建工具**: Vite
- **样式**: Tailwind CSS
- **UI 组件**: shadcn/ui (基于 Radix UI)
- **图标**: Lucide React

## 快速开始

### 本地开发

```bash
# 安装依赖
pnpm install

# 启动开发服务器
coze dev

# 或手动启动
pnpm dev
```

打开浏览器访问 [http://localhost:5000](http://localhost:5000)

### 构建生产版本

```bash
pnpm run build
```

构建产物位于 `dist/` 目录。

### 预览生产版本

```bash
pnpm preview
```

## 部署到 GitHub Pages

详细的部署步骤请查看 [DEPLOY_GUIDE.md](./DEPLOY_GUIDE.md)

简要步骤：

1. 在 GitHub 上创建仓库
2. 推送代码到 GitHub
3. 在仓库设置中启用 GitHub Pages（选择 GitHub Actions）
4. 等待自动部署完成
5. 访问 `https://yourusername.github.io/resume/`

## 自定义简历

简历内容位于 `src/components/Resume.tsx`，你可以修改以下部分：

- 个人信息（姓名、联系方式等）
- 个人简介
- 技能栈
- 工作经历
- 项目经验
- 教育背景
- 荣誉奖项

### 修改颜色主题

在 `src/components/Resume.tsx` 中搜索颜色类名，如 `blue-600`，替换为其他 Tailwind 颜色：
- `green-600` - 绿色
- `purple-600` - 紫色
- `red-600` - 红色
- `indigo-600` - 靛青色

### 添加头像

1. 将头像图片放到 `public/` 目录
2. 修改 `src/components/Resume.tsx` 中的头像部分：

```tsx
<img
  src="/your-avatar.jpg"
  alt="你的名字"
  className="w-32 h-32 rounded-full object-cover border-4 border-white/30"
/>
```

## 项目结构

```
├── .github/
│   └── workflows/
│       └── deploy.yml      # GitHub Pages 自动部署配置
├── public/                  # 静态资源目录
├── src/
│   ├── components/
│   │   ├── ui/             # shadcn/ui 组件
│   │   └── Resume.tsx      # 简历主组件
│   ├── lib/
│   │   └── utils.ts        # 工具函数
│   ├── App.tsx             # 应用入口
│   ├── main.tsx            # React 入口
│   └── index.css           # 全局样式
├── dist/                    # 构建产物
├── DEPLOY_GUIDE.md         # 部署指南
├── README.md               # 项目说明
└── vite.config.ts          # Vite 配置
```

## 许可证

MIT License - 自由使用和修改

---

如有问题或建议，欢迎提交 Issue 或 Pull Request。
